# Game Support Agent — 面试题自问自答

## 1. 为什么选择 LangGraph 而不是直接调 LLM API？

**因为我们需要有向无环图以上（有环）的控制流。**

直接调 LLM API 只能做到一问一答，处理不了多工具编排的场景。游戏客服需要这样一条执行链：

> 理解意图 → 可能多次查知识库 → 查账号 → 创建工单 → LLM 润色回复 → 安全检查

这就是个 ReAct 循环（Reason + Act），天然需要图编排。选 LangGraph 而不是 LangChain Agent 的原因：

- **显式节点**：7 个节点（reasoning / tool_exec / generate / detector / human / human_handoff / finish）每个职责清晰，调试时可以精确知道在哪一步出了问题
- **条件边可控**：`route_from_reasoning` 和 `route_from_tool_exec` 两条条件边由代码逻辑控制，不是黑盒 prompt
- **Human-in-loop 原生支持**：`interrupt()` / `Command(resume=)` 是 LangGraph 内建能力，不需要自己实现轮询或 WebSocket 状态同步

## 2. 人工审核（Human-in-loop）是怎么实现的？为什么不用外部审核队列？

**核心是 LangGraph 的 `interrupt()` 机制。**

当 LLM 判断需要转人工（调用 `request_human_escalation` 工具），流程是：

1. `tool_exec` 检测到该工具调用，设 `interrupt_info`
2. `human_handoff` 节点整理对话上下文和工具执行记录
3. `human` 节点调用 `interrupt()` 挂起整个图，返回中断载荷给 API
4. pending_store 记录该 session 等待审核
5. 审核员通过 `POST /api/v1/human/review/{session_id}` 提交回复
6. API 调用 `Command(resume=reply)` 恢复图执行
7. `finish` 节点检测到 `human_reply`，作为最终回复返回

**为什么不自己写审核队列？** 因为 `interrupt()` 做了最关键的事：**保存当前执行现场**（checkpoint）。如果用外部队列，需要自己序列化整个图状态、存起来、恢复时反序列化，这等价于重新实现 LangGraph 的 checkpointer。用 `interrupt()` 省掉了整个分布式状态同步的复杂度。

## 3. 为什么 pending_store 从内存改为 Redis？

**防止服务重启导致待审核任务丢失。**

原实现用进程内 dict，重启后所有等待审核的会话全部丢失——这在生产环境不可接受。Redis 方案：

- 24 小时 TTL 自动过期，防止堆积
- `decode_responses=True`，避免 bytes/str 转换问题
- `redis.asyncio` + 异步接口，不阻塞 FastAPI 事件循环
- 内存 dict 作为降级兜底：Redis 不可用时服务不中断

关键设计决策：**做降级而非强依赖**。如果 Redis 启动失败，pending_store 自动回退到内存模式，系统仍然可用，只是重启后数据丢失的风险回升。

## 4. checkpointer 为什么从 SQLite 换成 RedisSaver？

LangGraph 的状态持久化（checkpointer）原本用 `AsyncSqliteSaver`，每次图执行产生 checkpoint 都写 SQLite 文件。问题是：

- **性能瓶颈**：写 SQLite 是文件锁 + 磁盘 IO，多线程场景 contention 高
- **无 TTL**：checkpoint 无限累积，文件越来越大的同时读性能下降
- **水平扩展难**：SQLite 文件绑定在单个进程

换成 `RedisSaver`（`langgraph-checkpoint-redis`）后：

- 基于 Redis 纯内存操作，写入 ~微秒级
- TTL 自动过期旧 checkpoint
- 跨进程共享同一份状态（Docker 多副本部署可行）

代价是引入了一个新中间件依赖，但 Docker Compose 一键部署，运维成本可接受。

## 5. sensitive word detection 为什么不做成 interrupt？

**因为敏感词命中不需要人工介入。**

典型场景：用户不小心说了脏话，LLM 回复里带了敏感词。这时应该静默替换回复内容 + 记录日志，而不是把审核员叫来人工审。区分两条路径：

| 场景 | 处理方式 | 原因 |
|---|---|---|
| 敏感词命中 | detector 节点静默替换回复 | 不需要审核员介入 |
| LLM 主动要求转人工 | interrupt() 挂起 | 业务需要人工判断 |
| 工具调用失败 | detector 静默替换回复为道歉 | 不需要审核员介入，用户体验第一 |

`detector` 节点是图结构中的固定边，LLM **无法跳过**。这是双层兜底的第二层。

## 6. 27 道评测用例怎么设计？为什么分四类？

**覆盖全部边界场景，而不是测 happy path。**

四个类别对应 Agent 的四个核心能力：

| 类别 | 题数 | 测什么 | 典型 bad case |
|---|---|---|---|
| RAG | 7 | 知识库查询准确度、降级行为 | LLM 在知识库无结果时编造答案 |
| Tool | 8 | 工具选择、参数传递、重复调用 | 反复查同个账号、该创建工单时没创建 |
| HIL | 7 | 转人工触发时机、安全兜底 | 涉及敏感话题时直接回复了 |
| Multi-turn | 5 | 跨轮上下文、摘要传递 | 第二轮忘记第一轮说过什么 |

评测分三段打分：

1. **硬评分**：工具调用召回率、转人工标记是否命中、禁止操作是否触发（触发即 0 分）
2. **LLM-as-Judge**：用 `qwen-max` 评估回复对标准答案信息点的覆盖度
3. **综合加权**：工具 30% + 升等 15% + 禁止 25% + 内容 30%

防止过拟合的策略：不只看"最终回复是否正确"，还要看"是否调用了正确的工具"、"是否在错误的场景下调用了工具"。

## 7. streaming 响应 3.9s 是怎么做到的？

两个优化：

- **关闭 LLM thinking mode**：DashScope 的思考链输出非常耗时（~70s → ~3.9s）
- **`stream_mode="updates"`**：按节点粒度推送，不是 token 粒度。每执行完一个节点就推一个 event 给前端，体验上"一直在出东西"，实际等待时间就是 LLM 推理延迟

不逐 token 流式的原因是：工具调用场景下，最终回复由 `generate` 节点一次性产生，之前的 `reasoning` 和 `tool_exec` 阶段没有用户可见的文本输出。逐节点推送比逐 token 推送更匹配这个架构。

## 8. 这个项目如果上生产，最大的风险是什么？怎么缓解？

**最大的风险是 LLM 的不可预测性。**

即使有 safety detector 兜底，LLM 的回复仍然无法 100% 保证安全合规。缓解措施按优先级：

1. **图结构兜底**（已实现）：detector 节点是固定边，LLM 无法跳过
2. **Human-in-loop**（已实现）：高风险操作强制中断审核
3. **评测持续回归**（已实现）：27 道用例每次变更后跑一遍
4. **生产级 checkpointer**（已实现）：RedisSaver，支持多副本部署
5. **限流 + 监控**（待实现）：LLM 调用量控制和异常检测

核心原则：**不要把 LLM 当作可靠系统，而是把它当作一个不可靠的组件，用工程手段兜底。**
